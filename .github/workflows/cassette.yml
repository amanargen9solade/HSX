name: invention

on:
  push:

jobs:
  decent:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        beast: ["sorrow", "brake", "defect", "movie", "candidate"]
        famous: ["footstep", "helpless", "manner", "gymnasium"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar cX.jar
